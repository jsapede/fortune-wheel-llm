name: fortune-wheel-llm
kind: exclusive
version: '1.1.0'
description: Weighted LLM fallback rotator (position+latency+quota) with llm-keypool-style cooldown and pure round-robin option. Works on any Hermes profile.
on_enable: fortune_wheel_llm.register
