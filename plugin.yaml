name: fortune-wheel-llm
kind: exclusive
version: '1.0.0'
description: Fallback rotator pondéré (position+latence+quota) pour Hermes ha-only, RR pur optionnel, avec cooldown exponentiel type llm-keypool, sans fuite de données.
on_enable: fortune_wheel_llm.register
